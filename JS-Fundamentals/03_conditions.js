//if  ,Else ,E






var age = 12; 

if (age < 18 || age >35) 
  console.log("bhaag yaha se kiyuuki teri age " + "se kam hai")  

fruits[6] = "mango"
  console.log(fruits[6]);
  console.log(Object.keys(fruits));
  console.log(fruits.length);


  const colors = ["red","yelloy","blue"];
  colors[3] = "purple";
  
// initialize variable i
let i = 1;

// loop runs until i is less than 4
while (i < 4) {
    console.log(i);
    i += 1;}

    let num = 0, sum = 0;

    // loop as long as num is 0 or positive
    while (num >= 0) {
    
        // add all positive numbers
        sum += num;
    
        // take input from the user
        num = parseInt(prompt("Enter a number: "));
    }
    
    // last, display sum
    console.log(`The sum is ${sum}`);

const shiftArr = function (arr){

}
arr = [1,2,3,4,5,]:
function shiftArr 
 
 let i = 1:

while (num =>1):{

}
console.log(3)